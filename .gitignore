node_modules/
artifacts/
cache/

# 私钥在这儿。永远不要提交。
.env
.env.local

# deployment.json **要**提交 —— 里面只有合约地址、chainId 和 ABI，全是公开信息，
# 而页面没有它就读不到链上任何东西。以前把它忽略掉，站一发出去就是一片空白。

# IPFS 打包产物，23MB，不进仓库（node scripts/ipfs-pack.js 随时能重算，CID 不变）
dist/
