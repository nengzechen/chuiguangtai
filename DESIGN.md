---
name: 垂光 The Descending Light
description: 正文背后什么都没有——一块极深的纯色地，加一束从视口外垂下来的光，到页面中部就散尽；唯一的纹理是防止渐变出条带的抖动颗粒。
colors:
  ground-hi: "oklch(0.205 0.010 170)"
  ground: "oklch(0.165 0.008 170)"
  ground-lo: "oklch(0.128 0.007 172)"
  shaft: "oklch(0.800 0.055 212 / 0.20)"
  stone: "oklch(0.176 0.020 186)"
  stone-deep: "oklch(0.124 0.018 190)"
  stone-lit: "oklch(0.256 0.026 184)"
  stone-face: "oklch(0.206 0.022 186)"
  glass: "oklch(0.120 0.020 190 / 0.74)"
  glass-thin: "oklch(0.120 0.020 190 / 0.48)"
  glass-edge: "oklch(0.900 0.014 96 / 0.13)"
  cut-lip: "oklch(0.860 0.016 100)"
  cut-floor: "oklch(0.092 0.018 192)"
  paper: "oklch(0.940 0.004 110)"
  paper-shade: "oklch(0.884 0.005 110)"
  paper-edge: "oklch(0.812 0.006 110)"
  graphite: "oklch(0.270 0.006 250)"
  graphite-mid: "oklch(0.430 0.006 250)"
  graphite-soft: "oklch(0.505 0.006 250)"
  lime: "oklch(0.965 0.008 96)"
  lime-mid: "oklch(0.862 0.014 96)"
  lime-low: "oklch(0.775 0.018 96)"
  lime-faint: "oklch(0.706 0.022 96)"
  seal: "oklch(0.520 0.172 32)"
  seal-deep: "oklch(0.430 0.150 30)"
  seal-light: "oklch(0.660 0.165 34)"
  seal-wash: "oklch(0.520 0.172 32 / 0.14)"
  region-n: "oklch(0.780 0.052 240)"
  region-s: "oklch(0.745 0.062 300)"
  region-e: "oklch(0.800 0.058 108)"
  region-n-dot: "oklch(0.680 0.055 240)"
  region-s-dot: "oklch(0.640 0.065 300)"
  region-e-dot: "oklch(0.705 0.060 108)"
  region-n-pit: "oklch(0.120 0.026 240)"
  region-n-bed: "oklch(0.270 0.050 240)"
  region-s-pit: "oklch(0.118 0.030 300)"
  region-s-bed: "oklch(0.256 0.056 300)"
  region-e-pit: "oklch(0.122 0.024 108)"
  region-e-bed: "oklch(0.268 0.046 108)"
  asterism-line: "oklch(0.800 0.016 100 / 0.62)"
  asterism-line-lit: "oklch(0.975 0.010 100)"
  asterism-star: "oklch(0.960 0.014 100 / 0.94)"
  edge-lit: "oklch(0.900 0.016 100 / 0.16)"
  edge-dim: "oklch(0.900 0.016 100 / 0.07)"
  sunk-1: "oklch(0.090 0.020 190 / 0.40)"
  sunk-2: "oklch(0.090 0.020 190 / 0.62)"
  sunk-3: "oklch(0.080 0.018 190 / 0.80)"
  rule: "oklch(0.900 0.014 96 / 0.14)"
  rule-strong: "oklch(0.900 0.014 96 / 0.30)"
  rule-seal: "oklch(0.520 0.172 32 / 0.45)"
  rule-paper: "oklch(0.270 0.006 250 / 0.18)"
  thumb: "oklch(0.50 0.020 190 / 0.55)"
  ok: "oklch(0.680 0.105 155)"
typography:
  display:
    fontFamily: "Noto Serif SC Carve, Songti SC, SimSun, Source Han Serif SC, serif"
    fontSize: "56px"
    fontWeight: 700
    lineHeight: 1.22
    letterSpacing: "0.01em"
  headline:
    fontFamily: "Noto Serif SC Carve, Songti SC, serif"
    fontSize: "38px"
    fontWeight: 700
    lineHeight: 1.3
    letterSpacing: "0.01em"
  title:
    fontFamily: "Noto Serif SC Carve, Songti SC, serif"
    fontSize: "20px"
    fontWeight: 700
    lineHeight: 1.4
    letterSpacing: "0.01em"
  body:
    fontFamily: "ui-sans-serif, -apple-system, PingFang SC, Helvetica Neue, sans-serif"
    fontSize: "14px"
    fontWeight: 400
    lineHeight: 1.85
    letterSpacing: "0"
  readout:
    fontFamily: "JetBrains Mono, ui-monospace, SF Mono, Menlo, monospace"
    fontSize: "38px"
    fontWeight: 700
    lineHeight: 1
    letterSpacing: "0"
    fontVariant: "tabular-nums"
  label:
    fontFamily: "JetBrains Mono, ui-monospace, SF Mono, Menlo, monospace"
    fontSize: "10px"
    fontWeight: 400
    lineHeight: 1
    letterSpacing: "0.08em"
rounded:
  cut: "2px"
  slab: "3px"
  thumb: "99px"
spacing:
  s1: "4px"
  s2: "8px"
  s3: "12px"
  s4: "16px"
  s5: "24px"
  s6: "32px"
  s7: "48px"
  s8: "64px"
components:
  seal-button:
    backgroundColor: "{colors.seal}"
    textColor: "{colors.paper}"
    rounded: "{rounded.cut}"
    padding: "13px 20px"
  seal-button-hover:
    backgroundColor: "{colors.seal-deep}"
    textColor: "{colors.paper}"
  line-button:
    backgroundColor: "transparent"
    textColor: "{colors.seal-light}"
    rounded: "{rounded.cut}"
    padding: "11px 18px"
  niche:
    backgroundColor: "{colors.cut-floor}"
    rounded: "{rounded.cut}"
    size: "26px × 30px"
  paper-mount:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.graphite}"
    rounded: "{rounded.slab}"
    padding: "18px"
  sunk-panel:
    backgroundColor: "{colors.sunk-2}"
    textColor: "{colors.lime-mid}"
    rounded: "{rounded.cut}"
    padding: "24px 16px"
  input:
    backgroundColor: "{colors.sunk-3}"
    textColor: "{colors.lime}"
    rounded: "{rounded.cut}"
    padding: "12px 14px"
---

# Design System: 垂光 The Descending Light

## Overview

**Creative North Star: "正文背后什么都没有"**

世界仍然是**把穹顶内壁展开成一面墙** —— 88 个刻位本来凿在圆顶内壁上，
四道肋环 12 + 20 + 26 + 30 = 88，把那四道弧拉直就是墙上的四道横列。
分环一个没动，术语、编号、天区分配、藏品元数据全部照旧。

**这一版改的是背景层本身该不该存在。**

背景做过三版，三版都被否掉。三版各有一套自洽的道理，但连起来看，
错的是**同一件事**，而且那件事跟概念无关：

| 版本 | 做法 | 结果 |
|---|---|---|
| 一 | 磨光的黑石墙，天是石面上一层 alpha 0.10 的倒影 | **太空**：一整页黑 |
| 二 | 开顶的深空：银河、星云、光谱色的星、光晕、闪烁、流星 | **太俗** |
| 三 | 铜版星图：投影网格、大圆、星等符号、点画的银河 | **还是俗** |
| 四 | 把那一层**拿掉** | 这一版 |

三版都是"深色底 + 满屏程序化纹理"。概念一次比一次讲得通，
结果一次比一次不像好设计 —— 这本身就是信号：**问题不在概念里。**

去看了公认做得好的站之后，答案很直白，而且和前三版的做法正好相反：

> **好的深色站，正文背后什么都没有。**
> Linear 的地是 `rgb(8,9,10)`，`background-image: none` —— 一整张纯色。
> basement.studio 是黑底加一个焦点物件，正文背后同样是空的。
> 它们的高级感来自字、间距、留白和内容本身，**不来自背景里那一层东西**。

廉价感的来源不是"纹理画得不够好"，是**正文背后有纹理**这件事本身。
三版都在往那一层加东西，所以三版都往同一个方向掉。

所以这一版把那一层拿掉，只留一件事 —— 项目自己的名字：

**垂光**：一束光从上面垂下来。

一束。极大、极软、形状是**竖的**，从视口外落下来，到页面 45% 高度就散尽。
没有第二个光源，没有星，没有网格，没有线，没有画布，没有任何会动的东西。
省下来的功夫全部花在两件看不见的事上：让渐变平滑到不出条带，
让光的落点避开正文。

三种材料没变：

- **地** —— 极深的中性，带一丝墨绿。它是"深"，不是"黑"。
- **石** —— 立在地上的板。刻位、面板、读数条长在石上。
- **纸** —— 你带走的那一份。把纸贴在名字上拓下来，那是拓片。

页面上凡是"属于所有人的事实"长在石上，凡是"你自己的那一份"长在纸上。
**这不是深色模式配浅色卡片，这是两种真实存在的材料。**

**确认拒绝的反面参照（四层）：**
一层是品类的默认答案 —— 发光星点铺满的宇宙底 + 居中大倒计时 + 霓虹强调 + 3D 旋转的藏品。
另外三层是**这个项目自己试过并且失败了的三版**（见上表）。
它们共同的教训只有一句：**要往背景里加东西之前，先问它能不能长在组件上。**

*方向合约：seed key `f0f93fb7`（名录墙，第 1 位）。本文件记录的是**已经建成的那一版**，在完工时从代码反写。*

**Key Characteristics:**
- **正文背后什么都没有。** 这是第一条，也是唯一一条能防止再掉回去的
- 地只有一种：一块纯色。整页不会因为你走到哪儿就换
- 整页**一个光源**，而且它在标题上方就散尽
- **没有 canvas、没有 rAF、没有视差、没有 JS 参与背景。** 背景是两条 CSS 规则
- 阅读方向是横的：板号索引横在页顶，四道列横着铺开
- 整套只有一种**动作色**（朱砂），且**主行动本身是一枚印**
- 中文是碑刻的明体，**不收字距**；等宽只给真实测量值

## Colors

### Primary
- **朱砂 Cinnabar** (`{colors.seal}`)：整套里唯一的饱和色。
  碑帖上唯一会出现的颜色就是印泥 —— 盖章的人在说"这一份经我手，认过了"。
  它只干两件事：标记**已经被认领的刻位**，和**能按下去的东西**。
  一旦它变成标题色或装饰色，这一页就塌成一张普通的深色落地页。
  `seal-deep` 是印泥被压实的那一圈边，**只做形不做字**，以及按钮按下去时的底
  —— 印被按下去是更深，不是更亮。
  `seal-light` 是深色底上要承载小字时才提到的那一级。
  `seal` 本身**永远不做字**（在地上只有 2.66:1）—— 它只做底和边。

### Ground
- **地 Ground**（`ground-hi` / `ground` / `ground-lo`）：整页的底，**一块纯色**。
  三档只是同一块地的上中下，差极小（Y 0.0088 → 0.0021）——
  **看不出是渐变**，只看得出页面有上下。
  不是纯黑：带一丝墨绿（色相 170）。纯黑会让上面所有东西看着像贴纸。
- **垂光 The Shaft**（`shaft`）：整页**唯一**的光源，也是唯一一处非纯色。
  形状是**竖的**（74% 宽 × 96% 高，中心在视口外 −12%）——
  横向铺开就变成"顶上一团泛光"，那是任何深色模板都有的东西；
  收窄之后它才读得出是**一束**，两侧和四角真的暗下去，页面才有方向。
  alpha 0.20 是**沿着光束逐点算**出来的，不是取一个峰值拍脑袋（见 The Contrast Floor Rule）。
- **颗粒**（`grain-opacity` = 0.035）：这一版唯一的纹理，
  而它的职责是**消除瑕疵，不是增加花样** —— 大面积暗部渐变在 8 位色下必然出条带，
  抖动是唯一的解。`feTurbulence` 内联成 data URI，`mix-blend-mode: overlay`：
  中灰不改变底色，只在正负两侧各抖一点点，所以它抬不动整体亮度。

### Neutral
- **石 Stone**（`stone` / `stone-deep` / `stone-lit` / `stone-face`）：立在地上的板。
  它比地亮一点点 —— 抬起来的东西接到的光多一点，仅此而已。
- **玻璃 Glass**（`glass` / `glass-thin` / `glass-edge` / `blur-glass`）：
  少数需要让底下内容透过来的面板（顶栏、灯箱）。它**压暗**背后的东西，不是加一层雾。
  ⚠️ 顶栏尤其要克制：它压在**垂光最亮的那一段**上 ——
  上一版给它 0.88 的深色玻璃，等于把整页唯一的光源挡掉，页面看起来又是死黑，
  而原因根本不在背景里。现在是 0.42 → 0.22 的渐隐。
- **刻 Carve**（`cut-lip` / `cut-floor`）：凹槽的上唇与槽底。
  **这两个值是一对，永远一起用** —— 只给亮不给暗，字就是浮在石头上的，不是刻进去的。
- **凹陷三档**（`sunk-1` / `sunk-2` / `sunk-3`）：面板、列表行、读数格、输入框都从这三档里取。
  深浅只表示"凿得多深"，不表示"换了一种材料"。
- **前景四级**（`lime` / `lime-mid` / `lime-low` / `lime-faint`）：深色底上的全部文字。
  色相 **96（暖象牙）**。这不是口味：带墨绿的地上压冷蓝白，两个冷色互相咬，整页发灰发脏。

### Paper
- **纸 Paper**（`paper` / `paper-shade` / `paper-edge`）：拓片。**不是米黄。**
  拓片纸是冷的骨白，偏一点灰绿（色相 110）。
  暖奶油底 + 高对比衬线 + 陶土色强调是这一行最老的那个默认答案，
  这一版从纸的真实颜色上就把它避开。
- **石墨 Graphite**（`graphite` / `graphite-mid` / `graphite-soft`）：纸上的字。
  拓包擦上去的痕，不是纯黑 —— 铅笔有冷的金属光（色相 250）。

### Tertiary
- **天区三色 Sky-region Trio**：北天 / 南天 / 黄道，按刻位号轮着分（`(seat-1) % 3`）。
  首页的墙、观星台的穹顶、藏品元数据用的是同一组值 —— 改一处要改三处，
  所以它只在 `tokens.css` 里定义一次。它是**分类色**，不做按钮、不做标题。
  三档各有岗位不可互换：**星点**（`region-n/s/e`）只给星图上的星；
  **图例点**（`region-*-dot`）同色相压暗一档，11px 的实心方块用星点亮度会糊成一片光；
  **龛底**（`region-*-pit` 槽底 / `region-*-bed` 被光扫到的台面）落在石头的亮度上。

### Named Rules

**The One Red Rule.** 整套系统只有一种饱和色（朱砂）。
审核测试：把页面上所有朱砂盖住，若还剩下第二种饱和色，这条就破了。
（天区三色不算 —— 它们是数据的分类，不是强调。）

**The Single-Ground Rule.** 背景只有**一块纯色**，加**一束光**。
段与段之间用一道横缝分开（1px，两端淡出去），不允许给任何一段单独换底色去"调气氛"。
背景是 `fixed` 的两条 CSS 规则 —— 没有画布、没有 JS、没有视差、没有任何随滚动变化的东西。

**The Stone-or-Paper Rule.** 一个元素要么长在石上，要么长在纸上，不许各取一半。
属于所有人的事实（墙、读数、规矩、纪年）在石上；你自己的那一份（干版、拓片）在纸上。
纸上用石墨三级，石上用前景四级，两套前景色**永不混用**。

**The Empty-Backdrop Rule.** 这是这一版唯一真正的新规矩，也是前三版失败的总结：

> **正文背后什么都没有。**

判据是可执行的，不是态度：

1. **背景层里不许有"内容"。** 没有星、没有网格、没有线、没有符号、没有图案。
   背景只允许是：一块纯色的地 + 一束光 + 一层用于抖动的颗粒。
   审核测试：`landing.css` / `styles.css` 里 `body::before` 的 `background`
   **只能有两个 `gradient()`**。出现第三个，多半就是又在往那一层加东西了。
2. **想加东西时，先问它能不能长在组件上。** 能，就长在组件上（墙、拓片、读数条、印）；
   不能，多半是不该有。前三版每一版都跳过了这个问题。
3. **只有一个光源。** 两个就开始像"氛围渐变"，四个就是第二版那张壁纸。
4. **光要让出正文。** 落点算过：中心在视口外 −12%，到 45% 高度基本散尽 ——
   读到正文时背后已经是干净的地。
5. **没有 canvas、没有 `requestAnimationFrame`。**
   背景不该有生命周期、不该有可见性管理、不该在标签页切换时需要被照顾。
   审核测试：`web/` 下不该再有任何为背景服务的 JS 模块。
6. **唯一允许的纹理是抖动用的颗粒**，而且它的职责是**消除**条带，不是增加花样。
   判断标准：把它关掉，应该只看到条带变多，不该看到"少了点东西"。

**The Contrast Floor Rule.** 前景**每一级**都要在它自己的底上过 4.5:1，
包括最暗的一级 —— 刻度上的数字也是字。

底线数字：`--lime-faint` 要在合成后的背景上过 4.5:1，反推出
**背景任意一点的相对亮度 `Y ≤ 0.0395`**。
垂光的 alpha 就是从这个数倒推的 —— 而且这一版可以**逐点算**，
因为背景只剩两条已知几何的渐变，不再需要读画布像素来估。

沿光束中轴（最亮的那一列，也就是全页最坏的情况）：

| 视口高度 | 光的 alpha | 合成 Y | `lime-faint` | `lime-low` |
|---|---|---|---|---|
| 0%（顶端，导航栏底色之下） | 0.177 | 0.0158 | 6.12 | 7.85 |
| 0%（顶端，无遮挡） | 0.177 | **0.032** | **4.90** | 6.28 |
| 8% | 0.148 | 0.0195 | 5.79 | 7.43 |
| 15%（导航下沿） | 0.123 | 0.0226 | 5.54 | 7.11 |
| 25%（标题） | 0.088 | 0.0157 | 6.13 | 7.86 |
| 45% 以下 | ≈0 | 0.0058 | 7.21 | 9.25 |

其余（与背景无关，历版继承）：

| 前景 | 底 | 实测 |
|---|---|---|
| `paper` | `seal` | 5.02 |
| `paper` | `seal-deep`（按下） | 7.32 |
| `graphite` / `graphite-mid` / `graphite-soft` | `paper` | 12.58 / 6.79 / **4.91** |

`--seal`（L 0.520）在深色底上只有 2.66:1 —— 它**永远不做字**，只做底和边
（`.btn-fill` 的印、`.skip`、`.seg.on`）。要在深色底上承载小字，提到 `--seal-light`。
审核测试：搜 `color: var(--seal)`，应当一处都没有。

**改动背景之后必须重算。** 这一版好算（两条渐变，纸笔就够），
但前几版是读画布像素量出来的，那条路上有两个坑，都踩过，
而且两个都**不报错、只给出看起来合理的错数字**：

- **取块的统计量要用中位数，不能用平均。** 平均会被块里那一两个亮点整个拽上去，
  量到的就不是"字底下的背景"。中位数正好滤掉孤立的极值。
- **不能去解析 `getComputedStyle().color` 的字符串。** Chrome 把 `oklch()` 原样吐回来，
  拿正则抓数字会得到 `rgb(0.965, 0.006, 250)` —— 一个亮蓝色，
  它长得足够像颜色，不报错，只会让整份报告全线不合格（第一次实测全站"失败"就是这么来的）。
  正确做法是**光栅化**：填一像素到 1×1 canvas 再 `getImageData` 读回 sRGB 字节。

层级差靠尺寸、字重、字距拉开，**不靠把字压暗**。
`--seal` 的 L 定在 0.520 而不是 0.545，就是因为 0.545 时纸白压上去只有 4.49:1
—— 差 0.01 也是不合格。`--graphite-soft` 同理从 0.560 压到 0.505。
`--lime-faint` 从 0.672 提到 0.706 是反方向的同一条：当时实测最紧处只剩 4.64:1，
过线 3% 不叫过线叫碰巧。**这一档不许退回去** —— 那份余量是留给光的。

## Typography

**Display Font:** Noto Serif SC 700（自托管子集，`scripts/subset-font.js` 构建，
`font-display: swap`，不 preload；兜底 Songti SC —— 同属明体，字形骨架对得上，换上来不跳版）
**Body Font:** 系统无衬线（`ui-sans-serif` / `-apple-system` / PingFang SC）—— 一个字节都不下载
**Label/Mono Font:** JetBrains Mono 400 / 700（自托管，**只声明拉丁 `unicode-range`**，
所以中文永远不会去等它加载，也不会落进它的兜底字形里）

**Character:** 展示字是**明体**。宋/明体是刻工发明的字 —— 横细竖粗、起收有棱，
因为那是刀在木头和石头上最省力的走法。碑上的字本来就长这样。
上一版用黑体 900 加负字距把标题收紧成一块，那是**仪器面板**上的做法；
这一版反过来：碑上的字**不收字距**（`--ls-carve: 0.01em`），一格一格排。
700 而不是 900：明体的竖画本来就重，再加粗会把横画的细挤没，刻痕的棱角就糊了。

### Named Rules

**The Font-Pipeline Rule.** `@font-face` 声明在 `tokens.css` 顶部，**三条，缺一不可**。
*上一版这三条一条都不存在* —— 子集每次 `npm run font` 都在构建，
JetBrains Mono 还挂着 preload，但浏览器实测 `document.fonts` 是空的：
中文标题一直落在 PingFang SC，等宽一直落在 SF Mono，那个 preload 预载了一个谁也没声明的文件。
改动字面后必须在浏览器里确认 `document.fonts` 里三个字面都是 `loaded`，
构建产物存在不等于它被用上了。

**The Real-Measurement Rule.** 等宽字**只给真实的测量值**：编号、地址、计数、余量、区块号、时间。
不拿它装技术感 —— 任何一段散文落进等宽都是这条被破了。
反过来也成立：`刻死 / 单向 / 只追加` 这类词标**不是**测量值，所以它们走明体不走等宽。

**The Carved-Chinese Rule.** 新增中文文案后必须重跑 `npm run font` 重新子集化，
否则漏掉的字会静默掉回系统字体，同一句话里出现两种字面。
`npm run smoke` 有一项专门校验覆盖率，**它连 HTML 注释里的字也算** ——
所以注释用词也要落在子集里，或者换个说法，别为一句注释把子集撑大。

## Layout

内容居中，最大宽度 1280px（首页）/ 1180px（作业页 `.wrap`）。
间距是 8 级刻度（4 / 8 / 12 / 16 / 24 / 32 / 48 / 64px）。

**阅读方向是横的。** 上一版的脊柱是左侧一根竖直的仰角尺（0° 在下、90° 在上，滚动即抬头）；
这一版换成**页顶一条横向的板号索引**：纪念墙是按板编号的，你沿着墙走，板号一块一块过去。
它 sticky 在页顶，当前板亮朱砂，走过的板留着刻痕色，没到的板是空的
—— 索引本身也是一面被走过的墙。窄屏上板名收掉，**板号必须留着**：索引没有号就不是索引。

**首屏的编排顺序是有意的：标题 → 墙 → 计数 → 读数。**
标题先说这是什么，墙立刻给出证据（还剩多少空位），计数把证据变成数字。
墙放在标题上面的话，访客第一眼看到的是一堆看不懂的方格。

**四道列不等长。** 12 席那道就是比 30 席那道短。
槽的尺寸是固定的（26×30px，≤900px 为 20×24，≤560px 为 15×18），
列有多长由它装了多少个决定 —— 把短列拉满宽会让槽变大，
而一面墙上的槽是一个规格，这是石匠的活。

**作业页站在同一面墙前**，所以地是同一块石头。
但首页的编排语言不搬进作业页：没有板号索引、没有整墙首屏。
你在墙前办事的时候，不需要仪器表演。

响应式断点（真实使用的）：1180 / 1040（首屏两列并一列）、900（导航隐藏、板名收掉、槽缩到 20px）、
820（对照表的行标签升为整行抬头）、720（纪年纵向堆叠）、640（读数条纵向）、
560（正文内边距收到 `--s4`、槽缩到 15px、星图隐藏）。
`overflow-x: clip` + `img, svg { max-width: 100% }` 是全局底线：藏品 SVG 的固有宽度是 500px。

## Elevation & Depth

墙上只有**两种立体关系**，所以阴影词汇只有两条：

- **凹槽 Groove**（`--groove`）：唯一"凿进去"的配方。
  必须读起来是凹的：上唇亮、下沿暗、槽底最黑。用在槽、七格刻度、读数条、输入框。
- **抬板 Slab**（`--slab`）：从墙面抬起来的板，**落影在右下**（因为光在左上）。
  只给纸：首页那块干版、藏品页那张拓片。

石面有四层材质，全部用渐变画、不下载贴图（这一层铺满全页，一张纹理图要下载，渐变不用）：

1. **板缝** —— 墙是一块一块拼起来的。竖缝按 `--panel-w`（`min(16.666vw, 280px)`）重复，
   和页顶那条六块板的板号索引**指同一批板**：索引说"第 03 块"，你在墙上就该看得见第 03 块。
   缝是凿进去的，所以左暗右亮（光从左上来）。改板宽必须两处一起改，否则索引和墙对不上。
2. **天光反射** —— 磨光的石面会照出天，也照出站在它前面的人。这是名录墙的机制本身。
3. **矿斑** —— 花岗岩里的长石与石英，磨光之后是一颗颗看得见的亮点。
   六个不同周期（290–520px）铺开，避免肉眼抓到重复的网格。
4. **颗粒** —— 两层交叉排线，角度差 45°。单向排线看起来是拉丝金属，不是石头。

*一整块纯色读起来是塑料。这四层里删掉任何一层，墙都会往"深色卡片"那边掉一档。*

### Named Rules

**The Light-From-Upper-Left Rule.** 任何新的凹陷、抬板、光源都必须在左上。
出现一处右上或正下方来的光，整页的材质就散了。

**The No-Ghost-Card Rule.** 一块面板只用**一种**层次语言：
要么一道 1px 密度线，要么一层 `sunk-*` 底，不要"1px 边线 + 一层宽阴影"。

**The Groove-Pair Rule.** `--cut-lip` 和 `--cut-floor` 是一对。
只画亮唇不画暗底，槽就是浮在墙上的贴纸；只画暗底不画亮唇，槽就是一个洞。

## Shapes

圆角只有两档：**2px**（`--r-cut`，几乎所有东西：槽、按钮、输入、芯片、印）
和 **3px**（`--r-slab`，只给纸）。
石头上的东西是**切出来的**，不是揉圆的；纸会卷边，但也只卷 3px。
99px 只出现在滚动条拇指上。**没有胶囊按钮，没有大圆角。**

上一版的槽是拱顶凹室（上两角起拱）—— 那属于圆顶。
展开成墙之后槽是**方的**：那是石匠切出来的口子。

轮廓语言是**矩形 + 密度线**：分隔用 1px 的 `--rule` 系列，
列表项之间用 `gap: 1px` 加底色差（不画分割线）。

签名几何仍然是**四道分环**：12 + 20 + 26 + 30 = 88。
这组分环在三处必须完全一致 —— 首页的墙（`landing.js` 读 `dome.js` 的 `RINGS`）、
观星台的穹顶（`dome.js`）、藏品元数据。
**首页把它们拉直了，观星台仍然是弯的** —— 展开的是首页那面墙，
穹顶本身没有被改写；`ring.sag` 依然是弧的来源，首页只是不取用它。

## Components

### 印 The Seal（签名组件）
主行动**是一枚印**，不是一个填了红色的圆角矩形。
朱砂底、纸白字、内圈一道 `seal-deep` 的边（印泥被压实的地方）、2px 角。
Hover **变深**不变亮：印被按下去是印泥被压实。
Active 再 `translateY(1px)` 加一道内阴影，整个陷进纸里。
`prefers-reduced-motion` 下取消位移。

### 板号索引 The Panel Index（签名组件）
sticky 在页顶的一条横向索引，六块板。每块板的上沿是一道刻线，
当前板换成朱砂 —— 索引上唯一的颜色。走过的板刻线加深，没到的板是空的。
采样点落在视口**上三分之一**，不是正中：一块板刚进视野就该算"你正站在它前面"了，
等它滚到正中才点亮，索引会一直比脚步慢半块。

### 墙与凹槽 The Wall & Niche（签名组件，`landing.js` + `dome.js`）
四道横列，88 个 26×30px 的方槽。空槽就是空的，看得见槽底；
刻过的槽里有**一张小星图**，星数就是那个刻位真实的主星数（`names.json` 的 stars，与藏品图同源），
连线走最近邻。星图只有 17×20px：**连线必须比星点暗一档**（`.60` 对 `.92`），
否则缩到那个尺寸就是一团糊在一起的麻。
被看的槽从墙面抬起来 2px，落影在右下。你自己的那个槽盖着一圈朱砂印。

### 拓片 The Rubbing（签名组件）
纸。全站只有两处：首页那块干版、藏品页整张。
藏品图装裱在纸上，右上角一方朱砂**入藏印**（纸白字压在朱砂上，5.02:1）——
它记录一件已经发生、撤不回来的事。
页面上其他地方都是石头，只有这里是纸，这个反差本身就是"它归你了"。

### 双通道读数表 The Pair（签名组件）
"台上只有两种东西"是一张**表**，不是两张卡片：
行标签只写一次，两栏落在同一条基线上，中间一条竖直密度线。
`display: contents` 把 `dl` 和行容器摊平，让 `dt` 和两个 `dd` 直接成为网格项，从而跨行对齐。
**窄屏上不能退成单列** —— 两个值上下堆叠，读者就分不清哪个是星屑、哪个是星座，
列的身份靠位置对住上面那两个抬头，塌了这张表就没有意义了。
「另一条路」那一行星屑是「没有」：**空格本身就是这张表最该说清楚的信息**。

### 门限 The Cut-off（签名组件）
全站唯一一条硬边。它不是段落的顶边 —— 它是"低于 30° 不做测光"那道门限本身，
所以它长在页面中间，把该收钱的和不该收钱的真的分开：线以上要付，线以下不收钱。
那句「观星台不认钱，只认你来过」**骑在线上**，因为它就是这条线。
线要满宽，缩成一截就又变回装饰了；被限宽的是那句话，不是线。

### 三条规矩 The Invariants
三条并列的不变量，**不给编号**。01/02/03 是顺序记号，安在并列的东西上会读成流程；
真正的序列在「怎么留名」那一板，数字留给那一段。
这里标的是每一条凭什么改不掉：`刻死` / `单向` / `只追加`，走明体不走等宽。

### 七格 The Seven Days
"拾满 14 枚要来七天"写成一句话读者会滑过去，摆成七格他会数。
用槽的语言画（`--groove`），因为它们和墙上那 88 个槽是同一件事：空的位置，等人来占。

### 刻痕志 The Chronicle
一层层钉上去的标牌：`gap: 1px` 的等宽元信息 + 正文块，左侧一道竖边，
**最新一块的左边变朱砂**。前面的每一块都在，永远不覆盖 —— 视觉上必须能看出"只追加"。

### 藏品图 The Collectible（签名组件，`scripts/gen-metadata.js`）

500×500，程序化、种子确定。**2136 张全部随这一版重画。**
重画在本地链上进行，当时无人持有 —— "同一编号永远同一张图"从这一版重新起算，
上主网后不得再动。

**这一版最重要的一条：图里一个字都没有。**

上一代每张图下面压着一条题记 —— 中文名、拉丁名、稀有度、编号、朱砂印。
那是**标本卡**的做法，不是藏品的做法：真正的生成艺术藏品
（Fidenza、Ringers、Autoglyphs、Punks）图里一个字都没有。
名字和属性属于 metadata，交易平台会自己渲染成卡片；烧进图里，
等于在画布上贴标签，而且那张标签在每一个缩略图里都占掉四分之一的画面。

顺带一个好处：**"不得引用本站字体、要能在 OpenSea 独立渲染"这条约束现在自动满足** ——
没有 text 就没有字体可依赖。`npm run smoke` 直接查根因（2136 张里 0 处
`<text>`、0 处 `font-family`），不再查"有没有写兜底字体"这个症状。

**三个正交的特征各管一件事**，这是"能不能在网格里逐个认出来"的全部：

| 特征 | 管什么 | 取值 |
|---|---|---|
| 恒星遗迹 `kind` | **形** | 7 种天体，7 种轮廓 |
| 光谱型 `spectrum` | **色** | O→M，真实的恒星色温序列：蓝紫到红 |
| 余温 `heat` | **强度** | 尺寸、辉光半径、细节密度 |

上一代 2048 张全是灰的，在网格里长得一模一样 —— 那才是真正的问题，
不是"哪张画得不够好"。

**七种形都是这个天体真实的样子**，不是七个抽象图案：
红巨星是占满画幅的球（临边昏暗 + 色球层 + 日珥）；行星状星云是空心的同心壳；
白矮星是满幅黑里一个极亮的点；中子星是两道扫出去的束加一圈侧对的吸积盘；
磁星画的是**磁场本身**；超新星残骸是一堆被激波拉出来的丝；
黑洞视界是黑盘 + 光子环 + 被引力透镜抬起来的吸积盘。

踩过并且写进代码注释的四个坑：

1. **红巨星不许画暗斑。** 深色圆点压在球上，眼睛立刻读成**陨石坑**，
   整颗星变成一颗卫星。只画亮的对流胞，而且要拉长。
2. **中子星的光束必须用径向渐变 + `userSpaceOnUse`。**
   `linearGradient x1=0→x2=1` 按对象包围盒算，斜三角形的包围盒和光束方向对不上，
   于是整道束**不衰减**，画出来是一根插穿星体的实心棒。
3. **磁星的场线必须是主角。** 画细画淡了，它就只是"又一颗白矮星旁边有几道划痕"，
   和中子星的区别整个消失。
4. **超新星残骸不许画闭合硬轮廓。** 规整的锯齿一眼是**饼干模子**，不是爆炸。
   结构交给丝，壳只留极淡的一层雾。

**星座的图案与穹顶上那个小龛是同一个算法**（`web/dome.js` 的 `asterismGlyph`）：
同一个刻位号、同一个主星数，连出来的折线一模一样。
上一代这两处是两套算法，墙上看到的图案和真正拿到手的藏品对不上 ——
那是这套东西最不该出错的地方，龛就是这枚藏品在墙上的位置。

⚠️ **藏品图可以发光，而且应该发光。**
站点的背景规矩是"正文背后什么都没有"（The Empty-Backdrop Rule）——
那条管的是**正文背后**。藏品图是内容本身，不是谁的背景，它的工作就是被看见。
两者不矛盾，别把页面的克制搬到画布上来。

## Do's and Don'ts

### Do:
- **Do** 让新的面板从 `--sunk-*` 里取底；要分段就用那道横缝，不要换底色。
- **Do** 让所有凹陷、抬板的光保持在左上。
- **Do** 在改动背景之后**重算对比度**，而不是看一眼截图觉得"还挺暗的"。
  这一版好算：两条已知几何的渐变，沿光束中轴逐点算就是全页最坏情况。
- **Do** 想让页面更有戏时，把功夫花在**组件**上：墙、拓片、读数条、印、字号对比、留白。
  背景层不是放东西的地方 —— 这是三次失败换来的唯一结论。
- **Do** 把新的强调需求交给尺寸、字重、字距或位置解决；朱砂的稀有本身就是它的作用。
- **Do** 判断一个元素属于石还是属于纸，然后**只用那一套**前景色。
- **Do** 新增中文文案后重跑 `npm run font`，并在浏览器里确认 `document.fonts` 是 `loaded`。
- **Do** 用 `gap: 1px` + 底色差做列表分隔，用 1px `--rule` 做区块分隔。
- **Do** 让藏品 SVG 只用系统字体栈，它们必须能在 OpenSea、钱包等任何地方独立渲染。
- **Do** 在 `prefers-reduced-motion` 下关掉全部位移与过渡。

### Don't:
- **Don't** 往背景层里加"内容"。这是这一版唯一真正的红线，
  也是**前三版被否掉的共同原因**：星、网格、刻线、符号、图案 —— 一个都不要。
  审核测试：`body::before` 的 `background` 只能有两个 `gradient()`。
- **Don't** 为背景写 JS。没有 canvas、没有 `requestAnimationFrame`、没有视差、
  没有可见性管理 —— 背景不该有生命周期。
- **Don't** 加第二个光源。两个开始像"氛围渐变"，四个就是第二版那张壁纸。
- **Don't** 让顶栏（或任何满宽的 sticky 元素）压一层厚玻璃在垂光最亮的那一段上 ——
  那会把整页唯一的光源挡掉，而症状看起来像"背景又变死黑了"，很容易查错方向。
- **Don't** 把光横向铺开。铺开就是"顶上一团泛光"，任何深色模板都有；
  收窄成一束，四角才真的暗得下去。
- **Don't** 让朱砂做标题色、装饰色或分类色；也不要引入第二种饱和色。
- **Don't** 把 hover 做成"变亮"。印被按下去是**变深** —— 这一条既是材料真实，
  也是对比度要求：`seal-light` 上压纸白只有 2.8:1。
- **Don't** 用降低亮度来制造层级 —— 前景四级已经到底了
  （最坏位置上 `lime-faint` 4.90:1）。
- **Don't** 给面板同时加 1px 边和宽投影（幽灵卡片）；外投影只给纸。
- **Don't** 把短列拉满宽，也不要给槽起拱 —— 拱属于圆顶，墙上的槽是方的。
- **Don't** 在标题上方加 eyebrow / kicker。该信息若是真数据，它属于板号索引或读数条。
- **Don't** 用 ✦ ● 这类字符充当图标 —— 图标一律手绘线性 SVG，统一 1.6–1.8 描边。
- **Don't** 改动那组分环（12/20/26/30）—— 改了它，同一个刻位在三个地方就落在三个位置上。
- **Don't** 把首页的编排语言搬进作业页：你在墙前办事的时候，不需要仪器表演。
